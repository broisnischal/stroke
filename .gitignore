# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
scripts/.keys.json


packaging/aur/discerns/src/
packaging/aur/discerns/pkg/
packaging/aur/discerns/*.pkg.tar.zst
packaging/aur/discerns/*.tar.zstlogs/

# Local secrets — never commit
.env
.env.local
.env.*.local

# Local agent/editor notes — kept on disk, not shipped in the repo
AGENT.md

# Personal SQL notebooks — these can embed real query results (customer data);
# they belong in your documents folder, never in the repo
*.sqlnb

# Build/tooling cruft that must never be committed
squashfs-root/
*.AppImage
src-tauri/src/**/*.tmp.*
dummy.txt
file.txt
**/.claude/phobos-state.md
